.superpowers/
node_modules/
dist/
*.log
builds/
.tmp-builds/
.observer.lock

# Economy pulls: keep latest.{json,md} as the current reference; ignore dated snapshots.
data/economy/snapshot-*.json

# Locally-saved trade2 floors (cache-first store; regenerated on demand).
data/economy/trade-cache.json

# Local Claude Code config + any injected tooling skills (not project content).
.claude/
